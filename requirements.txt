# Daily Briefing Agent dependencies
# These versions are verified to install and run on Python 3.12 + crewai 1.14.6.
# (crewai requires python-dotenv>=1.2.2 and pydantic>=2.11.9, so we match those ranges.)

crewai==1.14.6
anthropic==0.105.2        # CrewAI Anthropic provider - required or LLM init fails
tavily-python==0.7.25
feedparser==6.0.12        # [Phase 2] RSS source adapter
requests>=2.32            # [Phase 2] Slack webhook POST (also a crewai dep)
streamlit==1.58.0         # [Phase 2] web UI entry point (app.py)
agentops==0.4.21         # [Phase 2] observability dashboard + tracing
python-dotenv==1.2.2      # matched to crewai's requirement (>=1.2.2,<2)
pydantic==2.12.5

# Dev/test only (not needed at runtime)
pytest>=8.0
